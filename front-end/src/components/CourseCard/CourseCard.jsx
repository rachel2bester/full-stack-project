import React from 'react'

const CourseCard = (props) => {
    const {name, category, completionTime, price, author, description} = props;
  return (
    <div className='course-card'>
        CourseCard
    </div>
  )
}

export default CourseCard